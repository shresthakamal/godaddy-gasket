# {{{appName}}}

{{{appDescription}}}


Now start up the app.

```bash
cd {{{appName}}}

{{{installCmd}}}

{{{localCmd}}}
```

The app should now be accessible over https on port 8080 at:

```
http://localhost:8080
```
