const DEFAULT_VARIANTS = ['_default'];
const DEFAULT_TTL = 10 * 60 * 1000; // default cache time is 10 minutes
const DEFAULT_MAX_AGE = 10 * 60; // 10 minutes
const DEFAULT_MAX_STALENESS = 60 * 60; // 60 minutes
const DEFAULT_MAX_NUMBER_OF_ITEMS = 1000;

export {
  DEFAULT_VARIANTS,
  DEFAULT_TTL,
  DEFAULT_MAX_AGE,
  DEFAULT_MAX_NUMBER_OF_ITEMS,
  DEFAULT_MAX_STALENESS
};
